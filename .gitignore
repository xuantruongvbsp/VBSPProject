# Dependencies & build output
node_modules/
dist/
*.tsbuildinfo

# Claude / MCP / dev tooling
.claude/
.mcp.json
CLAUDE.md
plans/
_inspect/

# PII — published datasets generated from Báo cáo 31 (cả plain + gzip,
# bao gồm catalog Cán bộ + Điểm giao dịch)
public/published*.json
public/published*.json.gz

# PII — file PDF đính kèm Quyết định (sinh runtime trên máy owner)
public/decision-attachments/

# Test screenshots from MCP browser sessions (root-level)
overview-*.png
viewer-*.png
snapshot-*.png
khoanh-*.png

# Scratch / preview artifacts
.tmp/

# Secrets
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Excel data files (defense in depth)
*.xlsx
*.xls
*.XLSX
*.XLS
